# Clone the project and do the following
- Open .env.template and copy the content then create a .env file and paste inside
- run npm install
- Access the site at http://localhost:5173/

# Login credentials 
- Admin : (email: admin@example.com) and (password: 1234)
- User : (email: user@example.com) and (password: 1234)

**When signed in as a user, there are some features which will not be accessible and visible to you**